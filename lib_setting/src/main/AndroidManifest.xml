<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lib_setting">

    <application>
        <activity android:name=".PDRDirectionActivity"></activity>
        <activity android:name=".WiFiAppointActivity" />
        <activity android:name=".SettingActivity" />

        <receiver
            android:name=".LauncherReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.zzy.receiver.LAUNCHER" />
            </intent-filter>
        </receiver>
    </application>

</manifest>